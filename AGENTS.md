# Contribution Standards

Commit messages MUST follow Conventional Commits.

Accepted types: `feat`, `fix`, `docs`, `style`, `refactor`, `perf`, `test`, `chore`, `ci`, and `build`.

Use `type: description` or `type(scope): description`. Mark breaking changes with `!` after the type or scope.
