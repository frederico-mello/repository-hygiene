name: ci

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  test:
    name: test (${{ matrix.os }}, Python ${{ matrix.python-version }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v3
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install package
        run: |
          uv venv
          uv pip install pytest -e .

      - name: Run tests
        run: uv run pytest tests_package/ -q

  install-flow:
    name: install flow (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v3

      - name: Build package
        run: uv build

      - name: Find wheel file
        id: find-wheel
        run: echo "wheel=$(ls dist/*.whl | head -1)" >> "$GITHUB_OUTPUT"
        shell: bash

      - name: uvx install (ephemeral)
        run: |
          mkdir -p /tmp/consumer
          git init /tmp/consumer
          uvx --from ${{ steps.find-wheel.outputs.wheel }} repository-hygiene install /tmp/consumer
          test -f /tmp/consumer/auditoria.yaml
          test -f /tmp/consumer/.github/workflows/repository-hygiene.yml

      - name: uv tool install (persistent)
        run: |
          uv tool install --force ${{ steps.find-wheel.outputs.wheel }}
          uv tool list | grep repository-hygiene
          uv tool run repository-hygiene --version
